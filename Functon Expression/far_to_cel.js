// Convert Fahrenheit to Celsius:
// Create a function expression to convert a temperature from Fahrenheit to Celsius.

const farenHiteTocelcius = function(farenhite){
return (farenhite -32)*5/9;
}
 
console.log(farenHiteTocelcius(100))

